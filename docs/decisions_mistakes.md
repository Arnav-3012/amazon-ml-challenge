# Decisions & Mistakes
Log wrong turns the moment they are identified. Format: `date | item | why`.

## Right calls
- 2026-09-25 | Repo mirrors the submission zip from day 1 | packaging = copy; no late restructuring risk
- 2026-09-25 | Country is never a model feature | test has unseen France; a country feature would be out-of-distribution
- 2026-09-25 | Zip = whitelist copy (output/, code/business_entity_resolution/, Documentation_template.md) | personal/agent docs (CLAUDE.md, docs/) must never ship
- 2026-09-25 | Read TSVs with `quoting=csv.QUOTE_NONE` | every line has exactly 4 tab fields, but ~800 lines carry CSV-style quotes; default quoting risks merging rows. Quote chars stay as text noise for the normaliser
- 2026-09-25 | Paths resolve from the repo root via `__file__`, not cwd | `python -m src.x` runs from code/business_entity_resolution while data/output sit at the root (same layout in the zip)
- 2026-09-25 | Canonical matching space = ASCII-transliterated Latin; raw kept for the encoder | S1 is ASCII while India S2/S3 is ~25% native script (Revision 1a)
- 2026-09-25 | Normaliser precision guard = exact non-pair collision ppm from group counts, not 200k sampled random non-pairs | EDA C7 exact-name rate on random non-pairs was 0.00%; a sampled guard can't register a rule that merges businesses
- 2026-09-25 | Alias split keeps the RIGHT side as core_name | every mined `<brand> <kw> <name>` sample has the S1 name after the keyword; the left side is a generated brand
- 2026-09-25 | Legal form moved to its own `legal_form` column, not deleted | breakdown Leaf 1 trap: stripping legal suffixes merges "Acme Inc" / "Acme LLC"; eval reports core+legal collisions alongside core-only
- 2026-09-25 | Blocking score as sparse products chunked by an exact nnz bound (row cost = Σ posting lengths) | inverted-index retrieval without a custom index; memory is set by `nnz_budget`, not by data size; `--dry` gives the exact cost bound before any product (eda.py/normalise.py lesson)
- 2026-09-25 | Train blocking keeps a rank grid (m≤10, k≤60), candidates = grid cut by `finalize()` | budget curve without reruns; the cut nulls out-of-cut ranks/scores so train candidates equal a direct config run (test)
- 2026-09-25 | S1-centric top-k is per source (S2, S3 separately) | processing unit is country × source; stated in config
- 2026-09-25 | Skeleton spec amended: leading vowel → "a", non-initial y = vowel, digit tokens unchanged | spec rules as written fail its own assert istrn~eastern (istrn vs estrn); y: sistms/systems, keyr/care diverged; repeat-collapse would turn "1100" into "10"

- 2026-09-25 | Blocking A and C get adjacent-token bigrams + a rarest-2 fallback (config `bigrams`, `fallback_rarest`, default on; read only inside block.py so train = test) | 38–50% of S1 names are shared with another S1 (Revision 3), so many names are made only of common tokens; with a df cap of 1000 those names get no channel-A candidates. A bigram ("ganesh_traders") is far rarer than its parts. A record with no token under the cap keeps its 2 lowest-df linkable tokens. Fallback tokens only match records whose kept set also contains them, so their cost is bounded by fallback-row postings; `--dry` reports it, plus zero-survivor % with and without fallback
- 2026-09-25 | Product-cost bound uses the actual posting lengths (column counts of the built index), not df-table n1 | with fallback, a common token sits only in the rows that fell back, so n1 would overstate cost by orders of magnitude

- 2026-09-25 | Added combined channel X (score = A+B+C over channel-tagged tokens) | diag: per-channel ranking loses true pairs to same-score ties (38-50% S1 name collisions); widening m/k 5/30→10/60 bought +2.3pp PC for +100 cand/S1. One joint ranking lets address break name ties

- 2026-09-25 | Blocking budget chosen by perfect-matcher macro F0.5 ceiling, not PC | PC treats every pair alike; the metric is per S1 (1 of 3 missed = 0.909, singletons = 1). Ceiling picks the knee: X m5/k10, 0.9873 @ 31/S1 vs 0.9904 @ 154/S1

- 2026-09-25 | M4 features are computed for every candidate of a split, not only the 20% S1 subset | record-relative features (v − max over the record's candidate S1s, rank, margin) on a subset would see ~20% of each record's competitors, a train/test skew; the subset is a row filter in train.py instead
- 2026-09-25 | IDF features reuse per-country idf_{split} (test IDF from test), same as blocking | country only selects the lookup table, never a value; the split-to-split scale shift is ln(N_test/N_train) per token, cancels in Jaccard ratios, partly in raw blocking scores (relative features cover it)
- 2026-09-25 | Early stopping on the held-out fold kept, as specified | it picks the iteration on the rows the OOF is scored on: small optimism in OOF log-loss/F0.5. Clean alternative if OOF vs LB gaps: early-stop on a 10% group split of the training fold
- 2026-09-25 | OOF argmax effect is a lower bound | in OOF a record's competing S1s outside the 20% subset are absent; at test all are present
- 2026-09-26 | M5 feature A/B (5% S1, fold 0, fixed scorer, seed std ≈0.00007): +a +0.0013, +b +0.0028, +c +0.0050, +d +0.0015, +b+c +0.0071, +all +0.0097 → KEEP ALL 4 groups | complementary: a/d add +0.0026 on top of b+c, although each is below +0.002 alone

## Mistakes
- 2026-09-25 | Blocking v1 ranked each channel alone | per-channel top-m/k can't break ties: a name-only channel scores every same-name S1 equally, and the address evidence lives in another ranking. PC 90.3%. Should have scored the joint evidence first and kept the single channels only as recall add-ons
- 2026-09-25 | eda.py first version had unvectorized `.apply(axis=1)`/dict-loop paths over 5M+ row files, only ever smoke-tested at ~400-row scale | wasted 27+ min of the user's wall-clock time before being killed; should have sanity-checked op-count (rows x per-row cost) against real file sizes before declaring it ready, not just checked "does it run on a tiny sample"
- 2026-09-25 | First eda.py "audit every hot path" pass missed a `list(2.2M-entry dict)` rebuilt inside a 200k-iteration while loop (C7 negative sampling) — the real bottleneck, ~33 min alone | should have grepped for list()/dict() calls inside loop bodies specifically, not just `.apply(axis=1)` patterns; caught only on the SECOND killed run, wasting another ~12 min of the user's time
- 2026-09-25 | Revision 2 stated the ~1-2M brute-force ceiling applied to candidate pairs (44M), implying candidate scoring itself was infeasible brute-force | conflated the search-index cost (2.2M S1 queries × ~10M S2+S3 vectors, genuinely infeasible brute-force) with scoring already-generated candidate pairs (cheap, chunkable); the real conclusion — blocking must be ANN/inverted-index *by design*, not as a fallback — got buried under an overstated/misattributed number. See breakdown.md Revision 2a
- 2026-09-25 | Revision 2 claimed the country pre-filter gives a "~3-4x reduction for free" without deriving it from the actual class shares | should have computed `1/Σ(share²)` from the already-known US/India 60/40 train split before writing a number down; the real figure is ~1.9x on train, ~2.5x on test — meaningfully weaker than claimed, and not sufficient on its own to avoid ANN blocking. See breakdown.md Revision 2a
- 2026-09-25 | Revision 2 called the decision-layer assignment step "Hungarian" (one-to-one bipartite matching) when the EDA finding it cited (each S2/S3 -> ≤1 S1, each S1 -> many) is a many-to-one structure | Hungarian solves the wrong problem shape; the correct rule is per-record argmax-above-threshold, not a one-to-one matching algorithm. Propagated the wrong term into docs/phases.md's M2 closure entry too. See breakdown.md Revision 2a
- 2026-09-25 | normalise.py v1 used `list.eval` with non-elementwise exprs (filter/shift/cum_min), which polars runs once per row: smoke showed ~110us/row → ~560s per 5.3M-row file, ~45 min for 6 files + >1h ablation | same lesson as eda.py: cost per row was never estimated before writing. Rewritten: name kept as a string (regex for legal/honorific), drops = when→null + native `list.drop_nulls`, dedupe via one explode/group_by. Rule for this repo: inside `list.eval`, elementwise expressions only
- 2026-09-25 | REVERTED `country_marker` (strip "(india)/(france)") | ablation: +0.059pp name recall vs +0.209pp S1 name collision; "(India)" is organic in S1 (2%), stripping merges "X (India) Pvt Ltd" with "X Pvt Ltd". France's "(France)" (8% of test S1) has the same profile, so India is the proxy; revisit only with France evidence (M6)
- 2026-09-25 | REVERTED `amp_and` ("&"/"+" → "and") | ablation: turning it OFF raises name recall +0.133pp and lowers S1 collision 0.111pp; the generator drops/replaces "&" more than it spells "and", so letting punct delete "&"/"+" is better on both
- 2026-09-25 | REVERTED `landmark` (strip near/opp/behind + 1-3 tokens) | ablation: −0.021pp addr recall, +0.008pp collision; no measurable gain, and landmark tokens stay available to addr_tokens for blocking
- 2026-09-25 | `block_eval.shared_flags` rewritten from 12x eager explode+hash-join+group_by to one `list.eval` membership-test pass per channel (no explode) | the running M3b eval was taking 30+ min with no prior baseline; explode blows up row count (millions of pairs x avg tokens per list) before a join, run 3 channels x 4 (country,source) groups; `list.eval(elem.is_in(rare_frozenset))` does the same per-pair boolean directly on the list column. Verified against the old function on a hand-built + 500-row randomized case (exact `.equals()` match) before editing the eval-in-flight process's on-disk source; the running process already has the old module loaded in memory so it was not disturbed. Not yet timed on real data — confirm the speedup on the next `block_eval` run
- 2026-09-25 | Claimed per-channel ties were the main PC loss and built channel X on that | X moved PC only +0.6pp (rank-1 81.4% vs 82.2%). It was a guess from the rank-tail shape, not from miss causes; should have split misses by cause (token-level) before building. X is kept: it halves candidates at equal PC
- 2026-09-25 | Caught before shipping (strict-old-man L3 review): `base_pairs(sample)`'s first draft computed `card` (true-match count per S1) AFTER sampling, not before | card_bin feeds section 2 of blocking.md (PC by cardinality). Verified on synthetic data: sampling first made card wrong for 1,385/1,543 S1s (90%) in the test — e.g. true card 7 read as 3. Fixed: `card` now comes from `load_gt_pairs()` before any sampling; only the row selection (which pairs go into the join) samples. The row-selection-is-identical claim (same seed, same source frame) was re-verified and holds; only the aggregate-before-sampling part was missed on the first pass. Full (unsampled) runs were never affected — sample=0 takes the same path as the old code.
- 2026-09-25 | Proposed char 3-grams for the PC gap before the miss split existed | sampled eval: only 3/18,090 misses share no token; 50% share only over-cap tokens and 50% are rank cuts. Fix is name×address composite tokens in X, not char n-grams. Rule: split misses by cause before choosing a recall fix
- 2026-09-25 | First M4 `relative()` used fill_null(0) on columns built from numpy NaN | NaN is not null in polars: it survived and ranked 1st in its record (missing address = "best address"). Caught by the ml-code-sensei review + a synthetic check before any real run. Rule: numpy-built float columns need fill_nan(None) before null handling
- 2026-09-25 | M4 OOF macro F0.5 (0.9654 @ t=0.80, pooled) is NOT representative of test | LOCO: India gap -0.1295 (0.9515 in-dist -> 0.8221 LOCO), US gap -0.0235. Test has France (unseen at train, zero same-country rows) -> expect France to behave like a LOCO country, closer to ~0.82-0.90 than the pooled 0.9654. Ship M4 for the first LB read as planned, but read the LB number against LOCO, not pooled OOF. M6 (LOCO CV + France hardening) is the fix, already scoped for this
- 2026-09-25 | Retracted my own post-M4 claims: (a) "OOF 0.9654 is well above the 0.9873 blocking ceiling" was nonsense, OOF sits 0.022 below the ceiling; (b) "expect France ~0.82 (LOCO-like)" and "the test ceiling is lower than 0.9873" were own inference from India LOCO with no test evidence, and a 0.987 leader contradicts (b) | rule: the LB score of our own M4 submission is the only evidence on France; get it before claiming a France penalty
- 2026-09-25 | Target set: beat 0.986955, stretch 0.99; encoder (M7) moved in scope, as a blocking channel + a cross-encoder on uncertain pairs only | at 0.99 the loss budget is 0.010 and blocking alone costs 0.0076 at the best measured grid, so a matcher-only encoder cannot reach it; scoring all ~61M test pairs with a transformer is hours on the M4 Pro, while the uncertain band is ~1-2% of pairs
- 2026-09-25 | Candidate generation = wide inverted-index retrieval + a cheap learned pruner; output only the survivors. Replaces "widen blocking" | organiser update: smaller candidate sets rank higher. 20% OOF: top-10 by X_score loses 0.016 F0.5, while pruning by learned p to 4.5/S1 loses 0.0000, so the cut must be learned. The pruner uses only blocking outputs (scores/ranks/relative/counts) so it stays part of candidate generation and is not the matcher in disguise; candidate_pairs.tsv = exactly the pruned set the matcher scores
- 2026-09-25 | D0-5 uses models/lgb_final.txt on S1s outside the final-fit 50%, not the "saved fold models" of m5-strategy §3 | train.py cv() never saved fold boosters; retraining them breaks D0's no-training rule. Held-out S1s make lgb_final clean for the eval rows; competitor rows of in-sample S1s are the stated caveat. Fix for M5 step 2: save fold boosters in cv()
- 2026-09-25 | D0-4 max-p comparator = held-out train records (every candidate S1 outside the 50%), stratified by n_cand, not the OOF | OOF rows cover only the 20% subset's S1s, so a record's max p would miss ~80% of its competitors; test pair p was never saved, so both sides are re-scored on a 60k-record sample
- 2026-09-25 | [MISTAKE] Claimed "the simulation understates test density" (test denser than the D0-5 19% S1 drop) | wrong: dropping 19% of S1s leaves 4.68 / 0.81 = 5.78 S2/S3 records per S1, and test has 5.75. The simulation matches test density, so D0-5's −0.0038 is the orphan share of the gap, not a lower bound. Rule: derive the density a simulation implies before comparing it with test
- 2026-09-25 | diagnose.py D0-4 `held` used `is_in(final.implode())` inside `group_by().agg()` on 69M candidate rows | polars broadcasts the 1.1M-id list literal per group: 181 TB allocation, abort after D0-1..3 had finished. Fixed: top-level filter + anti-join (checked against a pure-Python reference on synthetic data). Rule: `is_in(<list literal>)` only in top-level filter/with_columns, never inside an agg
- 2026-09-25 | gate: variant pre -> v1 (m=10 unchanged, n held at 45, NOT raised to 60) | docs/blocking_v2.md sweep: pre is worse than v1 at every (m, n) — e.g. m=10,n=45: pre PC 90.24%/ceiling 0.9565 vs v1 PC 96.38%/ceiling 0.9873. n=60 (v1,m=10) ceiling is 0.9885, +0.0012 over n=45's 0.9873 — real but not a "flat ceiling" at 45 (ceiling rises monotonically n=40->60: .9868/.9873/.9878/.9885). sweep_n has no rows past 60 in blocking_v2.md, so no evidence either way past 60. n stays 45 until `cv_full --preflight` extrapolates n=60's train feature-matrix RSS; n=75/90 considered only if that extrapolation is <=15GB at n=60
- 2026-09-25 | gate.n: 45 -> 60 (decided; superseding the prior "hold" entry above) | n>60 remains gated on `cv_full --preflight`'s RSS extrapolation, not yet run
- 2026-09-26 | [MISTAKE] sibling.anchor_margins used `pl.col("pre").get(1)` for the runner-up | `.get(n)` hard-indexes and raises on a 1-row group (a record with one pool S1); crashed the train sweep after 45 min. Fixed: `.slice(1, 1).first()` (null -> fill_null(0)). Rule: never `.get(n)` inside a group_by agg
- 2026-09-26 | [MISTAKE] gate.apply called `all_features(split, m, delta, log)` against signature `(cand, split, m)` | TypeError the moment sibling.enabled=true. Fixed: read the cached `sibling_{split}_d{delta}/features.parquet` that sibling.build writes
- 2026-09-26 | [MISTAKE] gate.apply asserts `candidates_{split}_v1.parquet` but nothing wrote it | backed up by hand after checking train = 31.29 cand/S1 (the v1 set). gate's variant=v1 never reads that file (V1 is computed from X_rrank/X_srank); the file is a rollback copy only
- 2026-09-26 | [MISTAKE] features.py new asserts: sib_hit>0 checked on the LAST part only; train-vs-test column check ran on the train pass against the stale M4 test dir (fails once sib_* exists) | Fixed: sum over all parts; column check only on the test pass
- 2026-09-26 | [MISTAKE] eyeball.py ran pred_contrib over all 13.8M OOF rows when only section 3's 60 sampled rows use it | >1h wall, killed with no output. Fixed: sample first, contrib on the sample only. Rule: filter/sample before any per-row explanation call (pred_contrib, SHAP)
- 2026-09-26 | [MISTAKE] eyeball.py filtered country == "United States"; the data value is "US" | every US half and all of section 5 would have come out empty with no error. Rule: read a column's actual unique values before writing a literal filter on it
- 2026-09-26 | [MISTAKE] eyeball.py section 6 re-implemented mine_dict's vocab test (S1 tokens only, outer_coalesce, list.explode in a mixed select) instead of calling mine_dict.shared | wrong definition + two crashes. Fixed: call shared() directly
- 2026-09-26 | Handle/domain segmentation (greedy longest-match against S1 name vocab) and channel D (exact-address key) both dropped, no pipeline changes | Segmentation: 5/20 = 25% on eyeball.md §6 cases against core_name ground truth, 13/15 failures unrecoverable by construction (handle never contains the full core_name's words, e.g. gbh.com can't yield {bh,gateway}), 0/2 real accuracy on the only theoretically-possible cases -- killed regardless of algorithm. Domain-flag reroute (address-only routing for domain-shaped records): S1 has 0% domain-shaped names (0/2,206,821), only S2/S3 do (~4.07%/4.05%), and even there too small a slice to be worth a pipeline change. Channel D (token-sorted address key, cap 50, train standalone): 18.35% PC alone, 72.4% of blocks skipped as oversize (>50) -- structurally misses common/under-specified addresses; unlikely to move v2's 96.7% ceiling on the margin. Both evaluated standalone (scratchpad scripts, no repo edits) before this call
- 2026-09-26 | [MISTAKE] ab_test.py scored ~dropE over all 5 folds with a single fold-0 model (Data.predict gives p only to fold-k rows for boosters[k]) | baseline 0.236 instead of ~0.965; every delta meaningless. Fixed: scope = fold 0 & ~dropE + baseline sanity assert. Rule: an eval scope must be a subset of the rows the model actually scored; assert the metric lands near a known reference before trusting deltas
- 2026-09-26 | Deferred gate n=60 for Submit #1 | features on v1 (69M rows) already built; n=60 ≈132M rows → ~2× time + OOM risk (13.4GB → ~25GB) for ~+0.001. Revisit Sun if memory allows
- 2026-09-26 | Generator-inversion killed (composition-depth) | 81% of names / 94% of addresses unreachable by rewrite ops. Channel D and domain-name segmentation drops logged above
- 2026-09-26 | Submit #1 = stage-1 5-fold mean, t = OOF (b) best 0.75, v1 candidates | LB 0.967 vs OOF(b) 0.9746. The S1-dropout OOF did not predict LB better than M4's plain OOF (same -0.008 gap), so do not treat OOF (b) as a calibrated LB estimate; track the gap as a separate problem
- 2026-09-26 | No deeper lexical blocking (block_autopsy) | rec top-m / name-key / addr unions: +0.0005 to +0.0010 ceiling for +3.7 to +28 cand/S1; 61% of misses absent from every channel. Recall budget goes to a new signal (encoder), not wider m
